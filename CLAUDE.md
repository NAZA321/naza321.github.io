# CLAUDE.md

このファイルは、このリポジトリでコードを扱う際に Claude Code (claude.ai/code) へガイダンスを提供するものです。

## リポジトリの状態

このリポジトリ（`naza321.github.io`、`master` から配信される GitHub ユーザー/Pages サイト）は現在**空**です。最新のコミット（`rm all`）でトラッキングされていた全ファイルが削除されました。ソースコード、`package.json`、ビルド設定、README、CI のいずれも、過去・現在問わずこのリポジトリには存在しません。

削除前の `master` には、ビルド済みの静的出力（`index.html`、`404.html`、`assets/js/app.*.js`、`assets/css/0.styles.*.css`、`posts/`、`category/`、`tag/` など）のみが含まれていました。これは VuePress のビルド成果物に似た構成です。VuePress の設定ファイル、`package.json`、Markdown のソースはこのリポジトリにコミットされたことがなく、最終的にレンダリングされた HTML/JS/CSS だけが `master` に直接 push されていました。おそらく別のソースプロジェクトのビルド成果物をコピーしてきたものと思われます。

ソースが存在しないため、実行すべきビルド・lint・テストのコマンドはありません。ここにサイトを追加するよう依頼された場合は、まったく何もない状態から始めることになります。新たに静的サイトジェネレーターを導入する前に、どれを使うか（使うかどうか）をユーザーに確認してください。
